% Zachary Gardner
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{normal_setup}[2021/10/26 Custom Package]

% Helpful commands for writing a package
% \RequirePackage
% \DeclareOptions
% \ExecuteOptions{...}
% \ProcessOptions\relax

% Packages
% -----
\RequirePackage{amsmath}
\RequirePackage{amssymb}
\RequirePackage{amsthm}
\RequirePackage{enumitem}
\RequirePackage{fancyhdr}
\RequirePackage{hyperref}
\RequirePackage{url}
% -----

% Expand margins
% -----
\addtolength{\oddsidemargin}{-.75in}
\addtolength{\evensidemargin}{-.75in}
\addtolength{\textwidth}{1.5in}
\addtolength{\topmargin}{-.75in}
\addtolength{\textheight}{1.5in}
\linespread{1}
% -----

% Set colors for hyperlinks
\hypersetup{
    colorlinks = true,
    citecolor = green,
    filecolor = green,
    linkcolor = magenta,
    urlcolor = blue
}
% -----

% Renewed commands
% -----
\let\oldAA\AA % \AA can be renewed
\let\oldIm\Im
\renewcommand{\Im}{\textup{Im}}
\let\oldl\l % \l can be renewed
\renewcommand{\l}{\ell}
\let\oldL\L % \L can be renewed
\let\oldO\O % \O can be renewed
\renewcommand{\O}{\mathcal{O}}
\let\oldP\P % \P can be renewed
\let\oldRe\Re
\renewcommand{\Re}{\textup{Re}}
\let\oldS\S % \S can be renewed
\let\oldSp\Sp % \Sp can be renewed
\let\oldsubset\subset % \subset can be renewed
\renewcommand{\subset}{\subseteq}
\let\oldsupset\supset % \supset can be renewed
\renewcommand{\supset}{\supseteq}
% -----

\newcommand{\cc}{\oldsubset\!\oldsubset\!}

% New environments
% -----
\newenvironment{enum}[1]{
\begin{enumerate}[label=\textup{({#1*})}]
}
{
\end{enumerate}
}
% -----

% New page styles
% -----
\fancypagestyle{fancydate}{
\fancyhf{}\renewcommand{\headrulewidth}{0pt}\fancyfoot[L]{\footnotesize Last Updated: \today}
}
% -----

% New theorems
% -----
% Numbered
\newtheorem{theorem}{\\Theorem}

\newtheorem{claim}[theorem]{\\Claim}
\newtheorem{corollary}[theorem]{\\Corollary}
\newtheorem{definition}[theorem]{\\Definition}
\newtheorem{example}[theorem]{\\Example}
\newtheorem{exercise}[theorem]{\\Exercise}
\newtheorem{fact}[theorem]{\\Fact}
\newtheorem{lemma}[theorem]{\\Lemma}
\newtheorem{proposition}[theorem]{\\Proposition}
\newtheorem{question}[theorem]{\\Question}
\newtheorem{remark}[theorem]{\\Remark}

% Not numbered
\newtheorem*{claim*}{\\Claim}
\newtheorem*{corollary*}{\\Corollary}
\newtheorem*{definition*}{\\Definition}
\newtheorem*{example*}{\\Example}
\newtheorem*{exercise*}{\\Exercise}
\newtheorem*{fact*}{\\Fact}
\newtheorem*{lemma*}{\\Lemma}
\newtheorem*{proposition*}{\\Proposition}
\newtheorem*{remark*}{\\Remark}
\newtheorem*{question*}{\\Question}
\newtheorem*{theorem*}{\\Theorem}
% -----

\endinput